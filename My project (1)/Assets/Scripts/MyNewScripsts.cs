using UnityEngine;

public class MyNewScripsts : MonoBehaviour
{
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    void Start()
    {
        Debug.Log("æ»≥Á«œººø‰");
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
